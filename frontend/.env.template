# Application Environment
NODE_ENV=development

# Backend URLs
NEXT_PUBLIC_BACKEND=http://127.0.0.1:8000/ws
NEXT_PUBLIC_BACKEND_API_BASE=http://127.0.0.1:8000/api/v1
NEXT_PUBLIC_WS_URL=http://localhost:8000

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=<your-nextauth-secret>  # Generate using: openssl rand -base64 32
JWT_SECRET=<your-jwt-secret>  # Generate using: openssl rand -base64 32

# Google OAuth Credentials
GOOGLE_CLIENT_ID=<your-google-client-id>
GOOGLE_CLIENT_SECRET=<your-google-client-secret>

# Instructions:
# 1. Copy this file and rename it to '.env'
# 2. Replace all placeholder values (enclosed in <>) with your actual values
# 3. Never commit the actual .env file to version control
# 4. Generate secure random values for secrets using the commands provided
# 5. Obtain Google OAuth credentials from Google Cloud Console
